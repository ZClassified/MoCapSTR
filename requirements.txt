pyserial>=3.5
opencv-python>=4.8.0
customtkinter>=5.2.0
Pillow>=10.0.0
pygrabber>=0.2
av>=12.0.0
# tkinter is part of the Python standard library.
# On Linux, you might need to install it via system package manager: sudo apt-get install python3-tk
